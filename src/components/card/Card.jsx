import React from "react";
const Card = ({ value, to, className }) => {
  // const [count, setCount] = useState(0);
  // const handleClick = () => {
  //   setCount(count + 1);
  // };
  return <div>card</div>;
};
export default Card;
